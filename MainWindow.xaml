<Window x:Class="GraphPathfinder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GraphPathfinder"
        mc:Ignorable="d"
        Title="Graph Pathfinder" Height="600" Width="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="10">
            <TextBlock Text="Algorithms" FontWeight="Bold" Margin="0,0,0,5"/>
            <ComboBox x:Name="AlgorithmSelector" Margin="0,0,0,10"/>
            
            <Button x:Name="FindPathBtn" Content="Find Path" Margin="0,5" Click="FindPathBtn_Click"/>
            <Button x:Name="ClearBtn" Content="Clear Graph" Margin="0,5" Click="ClearBtn_Click"/>
            <Button x:Name="ExportBtn" Content="Export Path" Margin="0,5" Click="ExportBtn_Click"/>
            
            <Separator Margin="0,10"/>
            <TextBlock x:Name="StatusText" Text="Ready" TextWrapping="Wrap"/>
        </StackPanel>

        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="10">
            <Canvas x:Name="MainCanvas" Background="White" MouseLeftButtonDown="MainCanvas_MouseLeftButtonDown"/>
        </Border>
    </Grid>
</Window>
